package it.zuperman.support_trainer.security.session;

/**
 * Canonical names for attributes stored in the future server-side session.
 */
public final class SessionAttributeNames {

    public static final String AUTHENTICATED_AT = "authenticatedAt";

    private SessionAttributeNames() {
    }
}
